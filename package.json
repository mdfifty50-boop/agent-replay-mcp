{
  "name": "agent-replay-mcp",
  "version": "0.1.0",
  "mcpName": "io.github.mdfifty50-boop/agent-replay",
  "repository": {
    "type": "git",
    "url": "https://github.com/mdfifty50-boop/agent-replay-mcp"
  },
  "description": "MCP server for agent session recording and replay — debug non-deterministic agent behavior with session comparison and divergence detection",
  "type": "module",
  "main": "src/index.js",
  "bin": {
    "agent-replay-mcp": "src/index.js"
  },
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "test": "node --test src/**/*.test.js"
  },
  "keywords": [
    "mcp",
    "mcp-server",
    "agent-replay",
    "session-recording",
    "agent-debugging",
    "divergence-detection",
    "session-comparison",
    "ai-agent",
    "agentic-engineering",
    "model-context-protocol"
  ],
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.24.0"
  }
}
